import {todo} from 'ava';

todo('test that node context is available in the sandbox');
todo('test that rethinkdb is exposed as `r` in the sandbox');
todo('test that one can write a query as a string');
